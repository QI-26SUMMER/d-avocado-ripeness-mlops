# Dependencies (shared across local Windows/RTX4060 and GCP Linux/GPU)
#
# For GPU (CUDA) torch, install from the index matching your CUDA version:
#   pip install torch==2.6.0 torchvision==0.21.0 --index-url https://download.pytorch.org/whl/cu124
# (GCP Deep Learning VM images may already include torch -> just check the version)
torch==2.6.0
torchvision==0.21.0

# Pure-Python dependencies (OS-independent)
pandas==3.0.3
numpy==2.5.1
scikit-learn==1.9.0
pillow==12.3.0
PyYAML==6.0.3
openpyxl==3.1.5
matplotlib==3.11.0
scipy==1.18.0

# Optional: for iPhone HEIC photo inference (src/predict.py)
# pillow-heif
